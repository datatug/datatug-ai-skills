---
description: Show install instructions for the datatug CLI
---

Invoke the `datatug:install` skill. It shows the user how to install the `datatug` CLI and then waits for them to run the install in their own shell — do not execute installers yourself.
